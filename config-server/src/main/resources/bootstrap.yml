spring:
  application:
    name: configserver
  #profiles:
    #active: native
  cloud:
    config:
      server:
        git:
          uri: file://C:/dev/springBootProjects/spring-boot-kafka/config-server-repo
          clone-on-start: true
    fail-fast: true


spring:
  application:
    name: configserver
  #profiles:
    #active: native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mostaphakanoune/spring-cloud-config-server-repo
          default-label: main
          clone-on-start: true
    fail-fast: true